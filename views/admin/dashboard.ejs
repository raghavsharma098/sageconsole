<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Admin Panel</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .admin-header {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
            padding: 1rem 0;
            box-shadow: 0 4px 20px rgba(220, 53, 69, 0.3);
            margin-bottom: 2rem;
        }
        
        .admin-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .admin-logo {
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .admin-menu {
            display: flex;
            gap: 2rem;
            align-items: center;
        }
        
        .admin-menu a {
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .admin-menu a:hover {
            background: rgba(255,255,255,0.1);
        }
        
        .admin-stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .admin-stat-card {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border-left: 5px solid #dc3545;
        }
        
        .admin-stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.12);
        }
        
        .admin-stat-icon {
            font-size: 3rem;
            color: #dc3545;
            margin-bottom: 1rem;
        }
        
        .admin-stat-value {
            font-size: 2.5rem;
            font-weight: 800;
            color: #1e40af;
            margin-bottom: 0.5rem;
        }
        
        .admin-stat-label {
            color: #6c757d;
            font-size: 1rem;
            font-weight: 600;
        }
        
        .admin-section {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
        }
        
        .admin-section-title {
            font-size: 1.8rem;
            color: #1e40af;
            margin-bottom: 1.5rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .reports-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }
        
        .reports-table th,
        .reports-table td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid #e0f2fe;
        }
        
        .reports-table th {
            background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%);
            font-weight: 600;
            color: #1e40af;
        }
        
        .reports-table tr:hover {
            background: rgba(59, 130, 246, 0.05);
        }
        
        .compliance-badge {
            padding: 0.3rem 0.8rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
        }
        
        .compliance-excellent {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        
        .compliance-good {
            background: linear-gradient(135deg, #17a2b8, #20c997);
            color: white;
        }
        
        .compliance-average {
            background: linear-gradient(135deg, #ffc107, #ffdb4a);
            color: #212529;
        }
        
        .compliance-poor {
            background: linear-gradient(135deg, #dc3545, #e94a5a);
            color: white;
        }
        
        .admin-actions {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
        }
        
        .admin-btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .admin-btn-primary {
            background: linear-gradient(135deg, #dc3545, #c82333);
            color: white;
        }
        
        .admin-btn-secondary {
            background: linear-gradient(135deg, #6c757d, #5a6268);
            color: white;
        }
        
        .admin-btn-success {
            background: linear-gradient(135deg, #28a745, #20c997);
            color: white;
        }
        
        .admin-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
    </style>
</head>
<body>
    <header class="admin-header">
        <div class="container">
            <div class="admin-nav">
                <div class="admin-logo">
                    <i class="fas fa-shield-alt"></i>
                    Admin Panel
                </div>
                <div class="admin-menu">
                    <a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                    <a href="/admin/questions"><i class="fas fa-question-circle"></i> Questions</a>
                    <a href="/admin/compliance-scores"><i class="fas fa-chart-bar"></i> Compliance Scores</a>
                    <a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                </div>
            </div>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            <h1 style="font-size: 2.5rem; color: #1e40af; margin-bottom: 2rem; font-weight: 700;">
                <i class="fas fa-tachometer-alt"></i> Admin Dashboard
            </h1>

            <!-- Statistics Cards -->
            <div class="admin-stats-grid">
                <div class="admin-stat-card">
                    <i class="fas fa-building admin-stat-icon"></i>
                    <div class="admin-stat-value"><%= stats.totalCompanies %></div>
                    <div class="admin-stat-label">Total Companies</div>
                </div>
                
                <div class="admin-stat-card">
                    <i class="fas fa-clipboard-list admin-stat-icon"></i>
                    <div class="admin-stat-value"><%= stats.totalAssessments %></div>
                    <div class="admin-stat-label">Total Assessments</div>
                </div>
                
                <div class="admin-stat-card">
                    <i class="fas fa-file-alt admin-stat-icon"></i>
                    <div class="admin-stat-value"><%= stats.totalReports %></div>
                    <div class="admin-stat-label">Generated Reports</div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="admin-actions">
                <a href="/admin/questions" class="admin-btn admin-btn-primary">
                    <i class="fas fa-edit"></i> Manage Questions
                </a>
                <a href="/admin/compliance-scores" class="admin-btn admin-btn-success">
                    <i class="fas fa-chart-line"></i> View All Scores
                </a>
            </div>

            <!-- Recent Reports -->
            <div class="admin-section">
                <h2 class="admin-section-title">
                    <i class="fas fa-chart-bar"></i> Recent Compliance Reports
                </h2>
                
                <% if (recentReports && recentReports.length > 0) { %>
                    <table class="reports-table">
                        <thead>
                            <tr>
                                <th>Company</th>
                                <th>Company ID</th>
                                <th>Industry</th>
                                <th>Compliance Score</th>
                                <th>Generated Date</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recentReports.forEach(report => { %>
                                <tr>
                                    <td><strong><%= report.companyId.companyName %></strong></td>
                                    <td><%= report.companyId.companyId %></td>
                                    <td><%= report.companyId.industry %></td>
                                    <td>
                                        <span class="compliance-badge 
                                            <% if (report.reportData.complianceScore >= 80) { %>compliance-excellent
                                            <% } else if (report.reportData.complianceScore >= 65) { %>compliance-good
                                            <% } else if (report.reportData.complianceScore >= 50) { %>compliance-average
                                            <% } else { %>compliance-poor<% } %>">
                                            <%= report.reportData.complianceScore %>%
                                        </span>
                                    </td>
                                    <td><%= new Date(report.generatedDate).toLocaleDateString() %></td>
                                    <td>
                                        <a href="/admin/company/<%= report.companyId.companyId %>/report" 
                                           class="admin-btn admin-btn-secondary" style="padding: 0.3rem 0.8rem; font-size: 0.8rem;">
                                            <i class="fas fa-eye"></i> View
                                        </a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p style="text-align: center; color: #6c757d; font-style: italic;">No reports generated yet.</p>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>
