<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Sustainability Assessment Platform</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .dashboard-hero {
            background: linear-gradient(135deg, #065f46 0%, #059669 50%, #22d3ee 100%);
            color: white;
            padding: 3rem 0;
            margin-bottom: 2rem;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(44, 85, 48, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .dashboard-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="20" height="20" patternUnits="userSpaceOnUse"><path d="M 20 0 L 0 0 0 20" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="1"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
            opacity: 0.3;
        }
        
        .dashboard-hero .content {
            position: relative;
            z-index: 1;
            text-align: center;
        }
        
        .dashboard-hero h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            font-weight: 700;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }
        
        .company-info {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 1.5rem;
            flex-wrap: wrap;
        }
        
        .company-info-item {
            background: rgba(255,255,255,0.25);
            padding: 1rem 1.5rem;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            gap: 0.75rem;
            word-wrap: break-word;
            word-break: break-word;
            overflow-wrap: break-word;
            min-width: 0;
            flex: 1;
            border: 1px solid rgba(255,255,255,0.3);
            transition: all 0.3s ease;
            color: white;
            font-weight: 500;
        }
        
        .company-info-item:hover {
            background: rgba(255,255,255,0.35);
            transform: translateY(-2px);
        }
        
        .company-info-item i {
            color: rgba(255,255,255,0.9);
            font-size: 1.2rem;
        }
        
        .company-info-item strong {
            color: white;
            font-weight: 700;
        }
        
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border: 1px solid rgba(74, 124, 89, 0.1);
            position: relative;
            overflow: hidden;
            min-height: 160px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(135deg, #059669, #22d3ee);
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.12);
        }
        
        .stat-value {
            font-size: 1.8rem;
            font-weight: 700;
            color: #065f46;
            margin-bottom: 0.8rem;
            word-wrap: break-word;
            word-break: break-word;
            overflow-wrap: break-word;
            hyphens: auto;
            line-height: 1.3;
            padding: 0 0.5rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        
        .stat-label {
            color: #6c757d;
            font-size: 0.9rem;
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
            word-wrap: break-word;
            overflow-wrap: break-word;
        }
        
        .stat-icon {
            font-size: 1.5rem;
            opacity: 0.7;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .dashboard-hero h1 {
                font-size: 2rem;
            }
            
            .company-info {
                flex-direction: column;
                gap: 1rem;
            }
            
            .company-info-item {
                justify-content: center;
                text-align: center;
                min-width: auto;
                flex: none;
                padding: 1.2rem;
                font-size: 1rem;
            }
            
            .dashboard-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            
            .stat-card {
                padding: 1.5rem;
                min-height: 120px;
            }
            
            .stat-value {
                font-size: 1.5rem;
            }
        }
        
        @media (max-width: 480px) {
            .dashboard-hero {
                padding: 2rem 0;
                margin-bottom: 1.5rem;
            }
            
            .dashboard-hero h1 {
                font-size: 1.8rem;
            }
            
            .company-info-item {
                padding: 1rem;
                gap: 0.5rem;
                font-size: 0.9rem;
            }
            
            .company-info-item i {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>
    
    <main class="main-content">
        <div class="container">
            <!-- Enhanced Welcome Header -->
            <div class="dashboard-hero">
                <div class="content">
                    <h1><i class="fas fa-tachometer-alt"></i> Welcome to Your Dashboard</h1>
                    <p style="font-size: 1.2rem; opacity: 0.9;">Monitor your sustainability journey and track progress</p>
                    <div class="company-info">
                        <div class="company-info-item">
                            <i class="fas fa-building"></i>
                            <strong><%= company.companyName %></strong>
                        </div>
                        <div class="company-info-item">
                            <i class="fas fa-id-badge"></i>
                            Company ID: <strong><%= company.companyId %></strong>
                        </div>
                        <div class="company-info-item">
                            <i class="fas fa-industry"></i>
                            Industry: <strong><%= company.industry %></strong>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Enhanced Dashboard Stats -->
            <div class="dashboard-grid">
                <div class="stat-card">
                    <div class="stat-value">
                        <i class="fas fa-id-card stat-icon"></i>
                        <%= company.companyId %>
                    </div>
                    <div class="stat-label">Your Company ID</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-value">
                        <% if (assessment) { %>
                            <% if (assessment.status === 'submitted') { %>
                                <i class="fas fa-check-circle stat-icon" style="color: #28a745;"></i>
                                Complete
                            <% } else { %>
                                <i class="fas fa-clock stat-icon" style="color: #ffc107;"></i>
                                In Progress
                            <% } %>
                        <% } else { %>
                            <i class="fas fa-play-circle stat-icon" style="color: #6c757d;"></i>
                            Not Started
                        <% } %>
                    </div>
                    <div class="stat-label">Assessment Status</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-value">
                        <% if (report) { %>
                            <i class="fas fa-chart-pie stat-icon" style="color: #059669;"></i>
                            <%= report.reportData.complianceScore %>%
                        <% } else { %>
                            <i class="fas fa-chart-pie stat-icon" style="color: #6c757d;"></i>
                            --
                        <% } %>
                    </div>
                    <div class="stat-label">Compliance Score</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-value">
                        <% if (report) { %>
                            <%= report.downloadCount %>
                        <% } else { %>
                            0
                        <% } %>
                    </div>
                    <div class="stat-label">Report Downloads</div>
                </div>
            </div>

            <!-- Main Dashboard Content -->
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-top: 2rem;">
                <!-- Company Information -->
                <div class="card">
                    <h3 class="card-title">Company Information</h3>
                    <div class="company-info">
                        <div class="info-row">
                            <span class="info-label">Company Name:</span>
                            <span class="info-value"><%= company.companyName %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Email:</span>
                            <span class="info-value"><%= company.email %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Industry:</span>
                            <span class="info-value"><%= company.industry %></span>
                        </div>
                        <div class="info-row">
                            <span class="info-label">Registration Date:</span>
                            <span class="info-value"><%= new Date(company.registrationDate).toLocaleDateString() %></span>
                        </div>
                    </div>
                </div>

                <!-- Assessment Progress -->
                <div class="card">
                    <h3 class="card-title">Assessment Progress</h3>
                    <% if (assessment) { %>
                        <div style="margin-bottom: 1rem;">
                            <div style="display: flex; justify-content: space-between; margin-bottom: 0.5rem;">
                                <span>Status:</span>
                                <% if (assessment.status === 'submitted') { %>
                                    <span style="font-weight: bold; color: #28a745;">Completed</span>
                                <% } else { %>
                                    <span style="font-weight: bold; color: #ffc107;">In Progress</span>
                                <% } %>
                            </div>
                            
                            <% if (assessment.submissionDate) { %>
                                <div style="display: flex; justify-content: space-between; margin-bottom: 1rem;">
                                    <span>Submitted:</span>
                                    <span><%= new Date(assessment.submissionDate).toLocaleDateString() %></span>
                                </div>
                            <% } %>
                            
                            <div style="margin-top: 1rem;">
                                <% if (assessment.status === 'submitted') { %>
                                    <a href="/assessment" class="btn btn-secondary w-100">Retake Assessment</a>
                                <% } else { %>
                                    <a href="/assessment" class="btn btn-primary w-100">Continue Assessment</a>
                                <% } %>
                            </div>
                        </div>
                    <% } else { %>
                        <p>You haven't started your assessment yet.</p>
                        <a href="/assessment" class="btn btn-primary w-100">Start Assessment</a>
                    <% } %>
                </div>
            </div>

            <!-- Latest Report -->
            <% if (report) { %>
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Latest Assessment Report</h3>
                        <p class="card-subtitle">Generated on <%= new Date(report.generatedDate).toLocaleDateString() %></p>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 2fr; gap: 2rem; align-items: center;">
                        <div style="text-align: center;">
                            <div class="score-circle" style="--score-deg: <%= (report.reportData.complianceScore / 100) * 360 %>deg;">
                                <span class="score-text"><%= report.reportData.complianceScore %>%</span>
                            </div>
                            <p style="font-weight: bold; margin-top: 1rem;">Compliance Score</p>
                        </div>
                        
                        <div>
                            <h4 style="margin-bottom: 1rem;">Quick Summary</h4>
                            <p style="margin-bottom: 1rem;"><%= report.reportData.summary %></p>
                            
                            <div style="display: flex; gap: 1rem; margin-top: 1.5rem;">
                                <a href="/reports/view/<%= report.reportId %>" class="btn btn-primary">View Full Report</a>
                                <a href="/reports/download/<%= report.reportId %>" class="btn btn-secondary">Download Report</a>
                            </div>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <div class="card">
                    <div class="text-center">
                        <h3 class="card-title">No Reports Yet</h3>
                        <p>Complete your assessment to generate your sustainability report.</p>
                        <% if (assessment && assessment.status === 'submitted') { %>
                            <a href="/reports/generate" class="btn btn-primary">Generate Report</a>
                        <% } else { %>
                            <a href="/assessment" class="btn btn-primary">Start Assessment</a>
                        <% } %>
                    </div>
                </div>
            <% } %>

            <!-- Quick Actions -->
            <div class="card">
                <h3 class="card-title">Quick Actions</h3>
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem;">
                    <a href="/assessment" class="btn btn-primary">
                        <% if (assessment && assessment.status === 'submitted') { %>
                            Update Assessment
                        <% } else { %>
                            Continue Assessment
                        <% } %>
                    </a>
                    
                    <% if (report) { %>
                        <a href="/reports/view/<%= report.reportId %>" class="btn btn-secondary">View Latest Report</a>
                    <% } %>
                    
                    <% if (assessment && assessment.status === 'submitted') { %>
                        <a href="/reports/generate" class="btn btn-success">Generate New Report</a>
                    <% } %>
                </div>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
    
    <script src="/js/main.js"></script>
</body>
</html>
