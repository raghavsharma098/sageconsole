<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Admin Panel</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .admin-header {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            color: white;
            padding: 1rem 0;
            box-shadow: 0 4px 20px rgba(220, 53, 69, 0.3);
            margin-bottom: 2rem;
        }
        
        .admin-nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .admin-logo {
            font-size: 1.8rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .admin-menu {
            display: flex;
            gap: 2rem;
            align-items: center;
        }
        
        .admin-menu a {
            color: white;
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .admin-menu a:hover {
            background: rgba(255,255,255,0.1);
        }
        
        .report-section {
            background: white;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 8px 25px rgba(0,0,0,0.08);
        }
        
        .company-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
            margin-bottom: 2rem;
        }
        
        .info-item {
            padding: 1rem;
            background: linear-gradient(135deg, #f8fffe 0%, #e8f5e8 100%);
            border-radius: 8px;
            border-left: 4px solid #3b82f6;
        }
        
        .info-label {
            font-weight: 600;
            color: #1e40af;
            font-size: 0.9rem;
            margin-bottom: 0.25rem;
        }
        
        .info-value {
            color: #333;
            font-size: 1.1rem;
        }
        
        .score-display {
            text-align: center;
            padding: 2rem;
            background: linear-gradient(135deg, #f8fffe 0%, #e8f5e8 100%);
            border-radius: 15px;
            margin-bottom: 2rem;
        }
        
        .score-circle {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 2rem;
            font-weight: 700;
            color: white;
        }
        
        .recommendations-list {
            list-style: none;
            padding: 0;
        }
        
        .recommendations-list li {
            padding: 0.75rem;
            margin-bottom: 0.5rem;
            background: rgba(74, 124, 89, 0.05);
            border-radius: 8px;
            border-left: 3px solid #3b82f6;
        }
    </style>
</head>
<body>
    <header class="admin-header">
        <div class="container">
            <div class="admin-nav">
                <div class="admin-logo">
                    <i class="fas fa-shield-alt"></i>
                    Admin Panel
                </div>
                <div class="admin-menu">
                    <a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> Dashboard</a>
                    <a href="/admin/questions"><i class="fas fa-question-circle"></i> Questions</a>
                    <a href="/admin/compliance-scores"><i class="fas fa-chart-bar"></i> Compliance Scores</a>
                    <a href="/admin/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                </div>
            </div>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            <h1 style="font-size: 2.5rem; color: #1e40af; margin-bottom: 2rem; font-weight: 700;">
                <i class="fas fa-building"></i> Company Report Details
            </h1>

            <!-- Company Information -->
            <div class="report-section">
                <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                    <i class="fas fa-info-circle"></i> Company Information
                </h2>
                <div class="company-info">
                    <div class="info-item">
                        <div class="info-label">Company Name</div>
                        <div class="info-value"><%= company.companyName %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Company ID</div>
                        <div class="info-value"><%= company.companyId %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Industry</div>
                        <div class="info-value"><%= company.industry %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Company Size</div>
                        <div class="info-value"><%= company.companySize || 'Not specified' %></div>
                    </div>
                </div>
            </div>

            <!-- Compliance Score -->
            <% if (report && report.reportData) { %>
                <div class="report-section">
                    <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                        <i class="fas fa-chart-pie"></i> Compliance Score
                    </h2>
                    <div class="score-display">
                        <div class="score-circle" style="background: 
                            <% if (report.reportData.complianceScore >= 80) { %>linear-gradient(135deg, #28a745, #20c997)
                            <% } else if (report.reportData.complianceScore >= 65) { %>linear-gradient(135deg, #17a2b8, #20c997)
                            <% } else if (report.reportData.complianceScore >= 50) { %>linear-gradient(135deg, #ffc107, #ffdb4a)
                            <% } else { %>linear-gradient(135deg, #dc3545, #e94a5a)<% } %>">
                            <%= report.reportData.complianceScore %>%
                        </div>
                        <h3 style="color: #1e40af; margin: 0;">
                            <% if (report.reportData.complianceScore >= 80) { %>Excellent Compliance
                            <% } else if (report.reportData.complianceScore >= 65) { %>Good Compliance
                            <% } else if (report.reportData.complianceScore >= 50) { %>Average Compliance
                            <% } else { %>Needs Improvement<% } %>
                        </h3>
                        <p style="color: #6c757d; margin-top: 0.5rem;">
                            Report generated on <%= new Date(report.generatedDate).toLocaleDateString() %>
                        </p>
                    </div>
                </div>

                <!-- Report Details -->
                <% if (report.reportData.summary) { %>
                    <div class="report-section">
                        <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                            <i class="fas fa-file-alt"></i> Executive Summary
                        </h2>
                        <p style="line-height: 1.6; color: #333;"><%= report.reportData.summary %></p>
                    </div>
                <% } %>

                <!-- Strengths -->
                <% if (report.reportData.strengths && report.reportData.strengths.length > 0) { %>
                    <div class="report-section">
                        <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                            <i class="fas fa-thumbs-up"></i> Strengths
                        </h2>
                        <ul class="recommendations-list">
                            <% report.reportData.strengths.forEach(strength => { %>
                                <li><i class="fas fa-check-circle" style="color: #28a745; margin-right: 0.5rem;"></i><%= strength %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } %>

                <!-- Weak Areas -->
                <% if (report.reportData.weakAreas && report.reportData.weakAreas.length > 0) { %>
                    <div class="report-section">
                        <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                            <i class="fas fa-exclamation-triangle"></i> Areas for Improvement
                        </h2>
                        <ul class="recommendations-list">
                            <% report.reportData.weakAreas.forEach(area => { %>
                                <li><i class="fas fa-exclamation-circle" style="color: #dc3545; margin-right: 0.5rem;"></i><%= area %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } %>

                <!-- Recommendations -->
                <% if (report.reportData.recommendations && report.reportData.recommendations.length > 0) { %>
                    <div class="report-section">
                        <h2 style="color: #1e40af; margin-bottom: 1.5rem;">
                            <i class="fas fa-lightbulb"></i> Recommendations
                        </h2>
                        <ul class="recommendations-list">
                            <% report.reportData.recommendations.forEach(rec => { %>
                                <li><i class="fas fa-arrow-right" style="color: #3b82f6; margin-right: 0.5rem;"></i><%= rec %></li>
                            <% }); %>
                        </ul>
                    </div>
                <% } %>
            <% } else { %>
                <div class="report-section">
                    <div style="text-align: center; padding: 3rem; color: #6c757d;">
                        <i class="fas fa-file-alt" style="font-size: 4rem; margin-bottom: 1rem; opacity: 0.3;"></i>
                        <h3>No report available</h3>
                        <p>This company hasn't completed an assessment yet.</p>
                    </div>
                </div>
            <% } %>

            <!-- Back Button -->
            <div style="text-align: center; margin-top: 2rem;">
                <a href="/admin/compliance-scores" class="btn" style="background: linear-gradient(135deg, #3b82f6, #22d3ee); color: white; padding: 0.75rem 2rem; border-radius: 8px; text-decoration: none;">
                    <i class="fas fa-arrow-left"></i> Back to Compliance Scores
                </a>
            </div>
        </div>
    </main>
</body>
</html>
